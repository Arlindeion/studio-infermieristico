# Roadmap

Revisione documentale: 28 agosto 2026. Le ultime evidenze operative della preproduzione privata risalgono al collaudo P0 del 24–26 agosto 2026; la fotografia completa della configurazione Render resta quella del 23 agosto e lo stato social è documentato soltanto fino al 4 agosto. Queste evidenze datate non provano automaticamente lo stato successivo dei pannelli. Il checkpoint interno è il 15 settembre 2026 e richiede sia un sito tecnicamente pronto e sicuro sia una prima campagna online/social coerente con la nuova identità. Non è una scadenza pubblica.

La verifica del 23 agosto ha confermato il database Render alla revisione Alembic `d91e6b4f2a30`, `flask db check` senza nuove operazioni e configurazione staging valida. Tra il 24 e il 26 agosto D-084 ha registrato il superamento reale di SMTP, Calendar, prestazioni sanitarie e resilienza condivisa: invii e tracciamento, guasto SMTP senza perdita dati, creazione/modifica/cancellazione e recovery Calendar senza duplicati, modifica e cancellazione esterna con decisione prioritaria e pulizia finale dei dati sintetici. Il 27 agosto D-086 ha corretto localmente il blocco PostgreSQL della prima iscrizione corso con la revisione `e2f4a6b8c901` e ha reso autonoma la pagina 500; il 28 agosto D-092 ha aggiunto l’anagrafica `Pazienti` e la revisione `f4c8a2d7e901` per consentire recapiti inizialmente mancanti, mentre D-097 porta la revisione locale a `a6c9e1f4b802` con il registro privacy per pratica. La preproduzione resta da migrare e ricollaudare sul flusso corso. Il modal dei conflitti e la nuova anagrafica sono stati verificati a 390×844 e 1440×900 px; D-083 persiste i timestamp di audit in UTC e li mostra come `Europe/Rome`, ma richiede ancora una verifica post-deploy distinta. Il collaudo P0 non autorizza dominio, DNS o apertura pubblica.

## Gate interno del 15 settembre

Il checkpoint è superato soltanto quando sono veri entrambi i risultati:

1. le attività P0 di sicurezza, dati, produzione e collaudo sono concluse;
2. la campagna iniziale sulla consulenza del sonno è pronta, misurabile e sostenuta da landing, contenuti social e processo di gestione dei contatti coerenti.

La sola finalizzazione grafica non basta. Se un requisito P0 resta aperto, il sito non è pronto all'esposizione pubblica anche se la campagna è stata preparata.

## Criterio di priorità

- `P0`: necessario prima dell'esposizione pubblica.
- `P1`: importante per un lancio credibile, rinviabile solo consapevolmente.
- `P2`: evoluzione successiva alla validazione.

## Completato o già implementato

- Identità principale definita: salvia dello studio, tipografia, linea rossa e fotografia reale.
- Header salvia responsive con testo scuro, gerarchia corsi/sonno verificata, quiz `Da dove parto?` riconoscibile come strumento di orientamento e footer verde profondo.
- Homepage ridisegnata come racconto a scene singole, con corsi e consulenza del sonno in due schermate distinte e priorità visiva ai corsi. I quattro corsi sono raccolti nelle famiglie `Sicurezza` e `Nascita e crescita`; quando non esistono date future non annullate la raccolta di interesse chiude la scena corsi e la scena Date scompare. La scena nascita usa una firma continua per i cinque professionisti; metodo e testimonianza formano un'unica sequenza. L'iscrizione OPI resta nei contenuti professionali dedicati e non viene trattata come beneficio commerciale.
- Regia scroll della homepage estesa a sette scene, oppure otto quando esistono date: ogni snap desktop occupa una sola schermata senza scroll interno; sotto 1024×640 px, su mobile e con movimento ridotto lo scorrimento resta libero. I laptop tra 640 e 840 px di altezza usano una composizione verticale più compatta. Il filo e la staffetta tipografica sono stati rimossi. Il sistema ibrido collega le prime tre scene con cuore e cistifellea scontornati; dalla scena sonno in poi i passaggi condividono scala, fuoco e un completamento legato al contenuto: fotografia del team, linea del metodo, feed attività e scelte finali. Su desktop ogni passaggio dura 0,85 secondi con arresto esatto sullo snap; gli impulsi del trackpad non possono saltare una scena. La guida laterale si adatta alle scene presenti e le raggruppa nei capitoli `Orientarsi`, `Conoscere` e `Scegliere`. Header reattivo e navigazione di scena restano attivi. Il parallax della hero usa livelli definitivi ricavati dall'originale `SELENE-16.jpg`, con fondale fotografico e maschera per-pixel di capelli e spalle.
- Pagine interne ricondotte allo stesso linguaggio visivo della homepage senza replicarne lo snap: contenuti e servizi mantengono scroll libero con ritmo editoriale e avanzamento di lettura, moduli e questionari usano una regia quieta, conferme ed esiti hanno una composizione dedicata. Chi sono, FAQ, directory corsi e privacy non usano più griglie di schede equivalenti; la CTA mobile si nasconde quando l'azione corrispondente è già visibile.
- Transizione orizzontale fra le pagine pubbliche implementata senza router client-side: la destinazione copre progressivamente la pagina corrente dietro il bordo rosso, il ritorno alla homepage procede in senso inverso e il fallback mantiene sempre i link funzionanti. Le ancore verso altre pagine aprono lo stesso punto sia nell'anteprima sia nel documento definitivo.
- Landing `call-first` sul sonno infantile 0-12 mesi, con gerarchia responsive, prezzi leggibili e call gratuita come azione dominante. Nello snap sonno della homepage `Scopri la consulenza` apre direttamente il confronto delle tre formule, mentre `Prima parliamone` apre il calendario della call.
- Prenotazione breve della call sonno con slot provvisorio, controllo incrociato database/Calendar, gestione admin ed email di conferma.
- Questionario sonno privato sul sito, inviabile solo dopo la call e la scelta della formula.
- Prestazioni infermieristiche mantenute in un flusso separato.
- Metadati condivisi, canonical, Open Graph e dati strutturati principali.
- Tracciamento differenziato delle CTA predisposto per GA4 e subordinato al consenso.
- Iscrizioni a corsi collegate alle date, con data/ora separate dal luogo non modificabile dell’edizione. Il modulo minimo di ricontatto resta disponibile sia quando il calendario è vuoto sia quando le date pubblicate non sono compatibili, in questo caso con la tematica preselezionata. La directory mostra prima le edizioni future già organizzate e porta al modulo con la data preselezionata.
- Gestione in admin di tipologie, capienza e stati dei corsi; la creazione di una nuova edizione è separata dalla gestione e propone `S.C. Studio Infermieristico` come luogo modificabile. La sezione `Corsi` contiene soltanto edizioni non annullate dalla data corrente in avanti e una coda limitata a richieste non confermate, lista d’attesa e ricontatti; corsi passati o annullati sono raccolti in `Archivio corsi`, dove il riepilogo conserva confermati e posti confermati. Il passaggio dell’edizione a `Annullato` rimuove l’evento Calendar e avvisa automaticamente, una sola volta, i partecipanti confermati. Il filtro e il flusso `Open day` sono disponibili soltanto per `Corso di accompagnamento alla nascita`.
- Sezione admin `Pazienti` con elenco ricercabile, creazione e modifica di telefono, email e dati anagrafici, controllo dei possibili duplicati, storico delle pratiche collegate e registro privacy per pratica con stato e data. La conferma di un appuntamento o di un’iscrizione corso crea e collega automaticamente la scheda se manca; anche l’ingresso in lista d’attesa corso registra subito il richiedente come paziente, senza confermarne il posto. Le altre richieste corso non confermate non popolano l’anagrafica. I recapiti salvati nelle pratiche già esistenti restano una fotografia storica e non vengono riscritti dall’anagrafica.
- Flusso privato del percorso nascita con edizioni, incontri, iscritti, presenze ed export PDF.
- Vista mensile dell’agenda, flusso dedicato per aziende e gruppi e quiz locale `Da dove parto?`, implementati il 13 agosto 2026; le richieste organizzative generano attività, proposta tracciata e corsi riservati non pubblici.
- Lettura degli impegni Google Calendar e scrittura degli eventi associati.
- Registro operativo per errori parziali di email e sincronizzazioni, con nome della persona, del corso o dell’attività accanto all’ID quando la pratica esiste ancora.
- Configurazione SQLite locale e PostgreSQL-ready.
- Creazione dell'amministratore messa in sicurezza: nessuna credenziale predefinita, bootstrap esplicito e arresto della produzione in presenza di configurazione mancante o credenziale legacy prevedibile; test dedicati superati.
- Prima riorganizzazione della documentazione in `docs/`.

Le funzionalità già presenti devono comunque superare il collaudo pre-lancio: “implementato” non equivale automaticamente a “pronto per produzione”.

## Piano operativo numerato verso il lancio

Questa è la checklist operativa canonica del progetto. I numeri degli step non vanno riordinati o riutilizzati: negli aggiornamenti si modifica lo stato e si aggiungono note o evidenze. Stati ammessi: `Da fare`, `In corso`, `Completato`, `Bloccato`. Uno step è `Completato` soltanto quando è soddisfatto il relativo criterio di uscita.

1. **Definire policy e vincoli operativi — `In corso` (P0).** Durate, prezzi, rate, assistenza, cancellazioni, spostamenti, rimborsi e assenze della consulenza sonno sono stati approvati e registrati il 21 luglio 2026. Il 29 luglio è stato chiarito che Arzamed conserva sempre l'orario di fine effettivo e che le prestazioni non usano durate fisse: anche nell'admin del sito Selene sceglie la durata prima della conferma. Il 13 agosto D-072 ha chiuso l’incongruenza sulla capienza: il limite automatico è la capienza nominale, con una sola eccezione `+1` se una coppia prende l’ultimo posto; l’admin può superarlo con conferma e motivo. Resta la validazione professionale del testo contrattuale/recesso. **Criterio di uscita:** decisioni validate e registrate nelle fonti competenti.
2. **Mettere in sicurezza la creazione dell'amministratore — `Completato` (P0).** Le credenziali automatiche sono state eliminate; locale e produzione usano un bootstrap esplicito, la password minima è di 16 caratteri e la produzione rifiuta sia configurazioni incomplete sia la credenziale legacy prevedibile. **Evidenza:** test dedicati e suite completa superati il 20 luglio 2026. **Criterio di uscita:** soddisfatto.
3. **Definire l'infrastruttura di staging e produzione — `Completato` (P0).** Il 23 agosto, dopo conferma del costo di 13,30 USD/mese più eventuali imposte, sono stati creati in Francoforte il Web Service Starter `sc-studio-infermieristico-production` e il PostgreSQL Basic-256mb `sc-studio-infermieristico-production-db`, nuovo e senza dati commerciali o sanitari, con disco da 1 GB e PITR di tre giorni. Lo staging gratuito non è stato promosso. Il servizio resta sul sottodominio Render con Basic Auth, `noindex`, Auto Sync e auto-deploy disattivati, database non raggiungibile da Internet e nessun dominio personalizzato. **Criterio di uscita:** soddisfatto.
4. **Creare e verificare le migrazioni Alembic — `Completato` (P0).** La baseline `56dda7f5137f` crea lo schema iniziale; le revisioni successive gestiscono call sonno, rimozione WhatsApp e durata effettiva. Il 13 agosto `a13d8f7c2b40` aggiunge gli strumenti operativi dell’admin, `c84f2d1a9e70` la coda dedicata alle richieste di aziende e gruppi e `d91e6b4f2a30` normalizza in modo condizionale gli schemi SQLite legacy. Il 27 agosto `e2f4a6b8c901` porta `iscrizione_corso.data_corso` da `VARCHAR(20)` a `VARCHAR(255)`; il 28 agosto `f4c8a2d7e901` rende facoltativo `persona_corso.telefono` senza perdere le righe esistenti. L'avvio operativo usa `flask db upgrade` e non `db.create_all()`. **Evidenza:** upgrade da database vuoto e dalle revisioni precedenti, conservazione delle righe e `flask db check` locali superati; la preproduzione resta alla revisione verificata `d91e6b4f2a30` fino al prossimo deploy. **Criterio di uscita:** soddisfatto per il repository; l’applicazione in staging è parte dello step 10.
5. **Definire backup e ripristino — `Completato` (P0).** La produzione userà PITR Render e dump giornalieri cifrati sul PC dell'attività, con conservazione 14 giornalieri, 8 settimanali e 12 mensili; checksum, destinazione vuota e restore mensile sono obbligatori. **Evidenza:** backup e ripristino completi verificati su PostgreSQL 18.4 con dati sintetici il 20 luglio 2026; 12 tabelle e revisione Alembic recuperate correttamente. **Criterio di uscita:** soddisfatto.
6. **Preparare la configurazione di produzione — `In corso` (P0).** Il Blueprint separato è stato applicato il 23 agosto mantenendo `APP_ENV=staging`, Basic Auth, `noindex`, nessun dominio e nessun dato reale. Migrazioni, bootstrap, Gunicorn, `/healthz`, admin e `validate-config` sono riusciti; dopo il primo accesso le due variabili `ADMIN_BOOTSTRAP_*` sono state rimosse e il nuovo deploy ha conservato l'account. Tra il 24 e il 26 agosto la configurazione Zimbra e il secret file Calendar sono stati usati con successo durante l'opt-in intenzionale alle integrazioni reali. Il resoconto non fotografa però lo stato finale delle variabili del pannello dopo il collaudo; D-083, pubblicata su `main`, richiede inoltre verifica dopo il prossimo deploy. **Criterio di uscita:** riconciliare in sola lettura il pannello corrente, mantenere Basic Auth/`noindex`, validare la configurazione prevista per la fase corrente e verificare timestamp e log dopo il deploy senza dati personali superflui.
7. **Pubblicare uno staging privato — `Completato` (P0).** Lo staging HTTPS su Render conserva Basic Auth, indicizzazione disabilitata e PostgreSQL collegato in Francoforte. Il 29 luglio è stato distribuito manualmente il commit verificato `8a4ad84` di `codex/snap-homepage`: migrazione, bootstrap dell'admin esistente e avvio Gunicorn sono riusciti. `/healthz` è stato escluso dal limite globale; 55 richieste consecutive e i monitor del deploy hanno risposto `200` senza nuovi `429`. Dopo il controllo sono state rimosse dal pannello le due variabili `ADMIN_BOOTSTRAP_*` e la variabile WhatsApp non più usata; il riavvio ha confermato nuovamente admin esistente, bootstrap riuscito e health check stabile. Root, `robots.txt` e `/healthz` restituiscono rispettivamente `401`, `200` e `200`, con `X-Robots-Tag` globale e `Disallow: /`. Il 30 luglio Blueprint e Web Service sono stati allineati a `main`, mantenendo Auto Sync e auto-deploy disattivati. L'approvazione della modifica ha avviato comunque un deploy del commit `e4fea38`: è stato annullato subito e il deploy live è rimasto `8a4ad84`, senza variazioni di piani, dominio o integrazioni. Il Blueprint è stato poi disconnesso per revocare il relativo Sync Hook. Dopo l'aggiornamento del database alla revisione `4d8b2c7a91e6`, un tentativo di avvio del vecchio commit `8a4ad84` è fallito perché non conteneva la revisione; il deploy manuale del `main` `148ec36` ha riallineato codice e schema senza modifiche ai dati, con migrazione, bootstrap, Gunicorn e `/healthz` riusciti. Nella stessa data l'attività ha confermato di non aver mai inserito dati reali nel database di staging. Al controllo Safari del 30 luglio, servizio Free, branch `main`, auto-deploy disattivato, deploy `148ec36` Live e area admin protetta risultavano confermati; i controlli HTTP restituivano root `401`, `/healthz` `200`, `robots.txt` bloccante e intestazioni CSP, HSTS, anti-framing, `nosniff` e `X-Robots-Tag`. Lo stato live successivo non è deducibile dal repository e va riconciliato in sola lettura prima del prossimo deploy. **Criterio di uscita:** soddisfatto.
8. **Configurare e collaudare email e Google Calendar — `Completato` (P0).** Nel collaudo privato del 24–26 agosto una richiesta sintetica ha prodotto notifica amministrativa e conferma paziente ricevute su Zimbra, entrambe tracciate nell'admin. Calendar ha superato lettura, creazione, modifica, cancellazione, orario invernale `Europe/Rome`, salvataggi ripetuti e assenza di duplicati. Il primo test con Calendar ID errato aveva causato errori TLS, `502`, code 139 e riavvio del worker; dopo D-080, D-081 e D-082 la pratica è rimasta locale, l'indisponibilità è stata degradata, il retry reale del 25 agosto alle 19:19 ha riallineato `1/1` elementi e modifica/cancellazione esterne hanno aperto il conflitto prioritario fino alla decisione. Ripristino e annullamento sono riusciti senza duplicati. **Evidenza:** resoconto P0 del 24–26 agosto 2026; D-084. **Criterio di uscita:** soddisfatto.
9. **Collaudare il flusso delle prestazioni sanitarie — `Completato` (P0).** Nel collaudo privato del 24–26 agosto una richiesta sintetica è stata salvata, mostrata in admin, notificata allo studio, confermata con durata effettiva e sincronizzata su Calendar. Sono riusciti spostamento con aggiornamento dello stesso evento ed email al paziente, cancellazione ordinaria con rimozione Calendar ed email, guasto SMTP senza rollback e gestione della cancellazione esterna tramite ripristino oppure annullamento. Servizio, data, ora, durata, testi e tracciamento sono stati verificati; tutti gli appuntamenti sintetici sono stati eliminati al termine. Il domicilio resta fuori dalla prenotazione diretta. **Evidenza:** resoconto P0 del 24–26 agosto 2026; D-084. **Criterio di uscita:** soddisfatto.
10. **Collaudare corsi e lista di interesse — `In corso` (P0).** I test sintetici verificano tipologie, date e stati, iscrizione singola e di coppia, conteggio dei posti e regola `capienza + 1` sull’ultima prenotazione di coppia. Il 13 agosto sono stati aggiunti lista d’attesa, deroga admin motivata, duplicazione e unione delle edizioni, spostamento partecipanti, export CSV/PDF e sincronizzazione degli incontri del percorso nascita. Il 27 agosto la prima iscrizione reale in PostgreSQL ha rilevato il P0 `data_corso VARCHAR(20)`: D-086 lo corregge localmente con `VARCHAR(255)` e protegge anche la pagina 500 dal contesto corso mancante. D-088 uniforma inoltre il flusso email: all’invio scrive soltanto allo studio, mentre conferma admin, annullamento e spostamento generano le comunicazioni al partecipante senza mettere a rischio il dato locale. D-089 porta agenda e panoramica direttamente alla lista dell’edizione, rende correggibili sul posto gli errori del modulo e limita i dati del secondo partecipante alla scelta `Coppia`. D-090 estende la stessa garanzia all’archiviazione di un’intera edizione: corso o laboratorio vengono annullati prima delle dipendenze esterne, i partecipanti confermati ricevono l’avviso con i recapiti approvati e un errore SMTP non ripristina il dato. Il 28 agosto D-093 sostituisce l’invito email della lista d’attesa con un’attività di contatto telefonico e rende indipendenti il salvataggio delle modifiche all’edizione e la scelta di inviare l’aggiornamento ai partecipanti. Il collaudo D-084 prova l'infrastruttura SMTP/Calendar condivisa, non il percorso corsi. Prima di proseguire occorre distribuire il repository fino alla revisione head `a6c9e1f4b802`, verificare `flask db check` e ripetere la prima iscrizione singola; restano poi la giornata operativa completa e le comunicazioni reali. **Criterio di uscita:** giornata simulata e prove reali senza errori SQL, sovracapienza automatica o comunicazioni errate.
11. **Collaudare il percorso nascita — `Completato` (P0).** Il 29 luglio i test sintetici hanno verificato pagina e richiesta open day, modulo completo accessibile soltanto tramite link privato e sempre `noindex`, edizioni aperte/chiuse, nove incontri, iscrizione della coppia come un posto, capienza e riapertura dopo annullamento, creazione e aggiornamento delle presenze, rubrica, conferme email simulate ed export PDF. Il controllo finale della capienza usa un blocco di riga su PostgreSQL per evitare due iscrizioni simultanee sull'ultimo posto. **Evidenza:** suite completa di 164 test Python, 18 test JavaScript e `git diff --check` superata. **Criterio di uscita:** soddisfatto.
12. **Collaudare il flusso della consulenza del sonno — `In corso` (P0).** Il 29 luglio i test sintetici hanno verificato prenotazione qualificata, disponibilità nazionale incluso il sabato, UTM, blocco provvisorio Calendar, promemoria email senza duplicati, conferma, modifica, annullamento, call conclusa, scelta della formula, invito e accesso al questionario privato `noindex`. Il 23 agosto è stata corretta localmente la larghezza anomala dei primi due checkbox obbligatori su smartphone e aggiunto un test anti-regressione; il controllo a 390×844 e 1440×900 px è riuscito. Il collaudo D-084 prova l'infrastruttura SMTP/Calendar condivisa, non il percorso sonno. Restano deploy della correzione, prova end-to-end reale della call e definizione/collaudo del passaggio di pagamento. **Criterio di uscita:** flusso completo verificato con integrazioni reali e disponibilità nazionale invariata.
13. **Verificare la resilienza dei flussi — `Completato` (P0).** Nel collaudo privato del 24–26 agosto la password SMTP è stata resa temporaneamente invalida: richiesta e conferma sono rimaste salvate, Calendar è stato aggiornato e i due errori email sono comparsi in `RegistroEvento`. Il primo test Calendar con ID errato aveva invece causato `502` e crash del worker; dopo la correzione la dipendenza è rimasta degradabile, la pratica senza evento è rimasta valida localmente, il retry orario l'ha riallineata senza duplicati e ha inviato il riepilogo `1/1` riuscito. Modifiche e cancellazioni esterne hanno richiesto una decisione esplicita; ripristino e annullamento hanno chiuso correttamente il conflitto. Log e audit restano come evidenza, mentre i dati sintetici sono stati rimossi. D-083 conserva un controllo post-deploy separato sul solo rendering dei timestamp admin. **Evidenza:** resoconto P0 del 24–26 agosto 2026; D-084. **Criterio di uscita:** soddisfatto per l'infrastruttura condivisa; i collaudi funzionali specifici ancora aperti restano negli step 10 e 12.
14. **Validare privacy, cookie e trattamento dati — `In corso` (P0).** Il 29 luglio il contatto e la forma dell'indirizzo postale sono stati allineati ai dati operativi approvati. Il comportamento tecnico è coperto da test: prima dell'accettazione non viene richiesto lo script GA4; rifiuto e revoca negano Analytics/Ads, interrompono gli eventi di conversione e rimuovono soltanto i cookie Analytics. D-097 conserva inoltre nella scheda paziente stato, data e pratica di origine del consenso operativo senza attribuirlo retroattivamente ai vecchi appuntamenti. GA4 e Meta restano non configurati. Restano da verificare informative, basi giuridiche, responsabili esterni, consensi, conservazione, configurazione Analytics, questionario sonno, collegamenti esterni e dati sanitari con competenza professionale adeguata. **Criterio di uscita:** testi e comportamento approvati; GA4 e Meta non ricevono eventi prima del consenso.
15. **Finalizzare contenuti e autorizzazioni — `In corso` (P1).** Il placeholder pubblico della landing sonno è stato sostituito, le fotografie disponibili risultano autorizzate anche per social e ads e la qualifica `infermiera e consulente del sonno infantile` è verificata. Restano le fonti cliniche e le prime testimonianze reali. **Criterio di uscita:** nessun contenuto pubblico privo di autorizzazione o ancora provvisorio.
16. **Eseguire il collaudo visuale, accessibilità e SEO — `In corso` (P1).** Il 23 agosto un primo controllo della preproduzione privata non ha rilevato anomalie fuori dai checkbox della prenotazione sonno; la correzione è stata verificata localmente a 1440×900 e 390×844 px, con tre controlli allineati, testo leggibile e area di riga cliccabile. Il 26 agosto il modal D-082 è stato verificato alle stesse dimensioni con dati sintetici: confronto completo, nessun overflow di pagina, focus iniziale nel dialog, azioni mobile da almeno 44 px, rinvio per sessione e riproposizione al login successivo. Il 27 agosto la pagina 500 del modulo corsi è stata verificata senza dipendenze dal contesto della pagina originaria; il 28 agosto anche elenco e scheda `Pazienti` sono stati controllati a 1440×900 e 390×844 px, senza overflow e con azioni mobile da almeno 44 px. D-097 ha ripetuto il controllo sulla scheda popolata con consensi accettati e non registrati, senza errori console. Il 29 agosto D-098 ha verificato la separazione tra corsi attivi e archivio alle stesse dimensioni, con tabelle scorrevoli internamente e senza overflow globale; D-099 ha ripetuto il controllo sulla coda richieste, il riepilogo dei confermati e il filtro Open day condizionale. La suite locale passa con 253 test Python e 37 test JavaScript. Restano il collaudo completo degli stati popolati, la verifica post-deploy D-083 su un timestamp noto e il controllo sul dominio definitivo. **Criterio di uscita:** controlli documentati, difetti bloccanti risolti, `pytest`, test JavaScript e `git diff --check` superati.
17. **Preparare processo e misurazione commerciale — `In corso` (P1).** Tre formule, capacità, prezzi, KPI, limite settimanale delle call e tracciamento UTM sono definiti. Restano pagamento, stato cliente, misurazione Meta/GA4 con consenso e verifica del tempo dopo i primi cinque percorsi. **Criterio di uscita:** processo da richiesta a cliente pagante misurabile e sostenibile.
18. **Preparare la campagna iniziale — `In corso` (P1).** Test nazionale Meta/Instagram, budget 200 € con checkpoint a 100 €, due angoli iniziali, presenza di Selene e assenza di testimonianze simulate sono approvati. Al 4 agosto risultavano pubblicato un Reel organico sui risvegli, disponibili i girati originali del sonno e della disostruzione e ancora non individuati i master puliti e varie consegne finali. Questi materiali non soddisfano da soli il gate pubblicitario e il loro stato attuale va verificato. Restano master e varianti approvati per uso pubblicitario, copertine, configurazione tecnica e approvazione finale. **Criterio di uscita:** materiali approvati, CTA coerente e budget attivabile solo dopo la verifica del tracciamento.
19. **Eseguire il controllo finale pre-lancio — `Da fare` (P0).** Riesaminare sicurezza, migrazioni, backup, privacy, flussi, contenuti, misurazione e capacità operativa usando i criteri di uscita del progetto. **Criterio di uscita:** nessun P0 aperto o bloccato e decisione esplicita di procedere.
20. **Aprire il dominio pubblico e monitorare — `Da fare` (P0).** Collegare `scstudioinfermieristico.it`, confermato come dominio definitivo da D-074, quindi verificare HTTPS, cookie, redirect, indicizzazione, invii reali, Calendar, log e conversioni; attivare la campagna solo dopo la stabilità iniziale. **Criterio di uscita:** sito pubblico stabile, monitorato e pronto a ricevere richieste qualificate.

## Dopo il lancio

### P2 — validazione e ottimizzazione

- Misurare richieste qualificate, provenienza, tasso call/percorso e carico di lavoro.
- Verificare mensilmente l'obiettivo interno di almeno tre testimonianze reali e autorizzate sulla consulenza del sonno, senza trasformarlo in un obbligo per le famiglie.
- Rivedere creatività, geografia e messaggi durante i tre mesi senza modificare contemporaneamente tutte le variabili.
- Rivedere prezzi e formato della consulenza sulla base dei dati reali.
- Costruire una mailing list con consenso e segmentazione minima.
- Migliorare il processo di ricontatto e il calendario editoriale.
- Creare landing verticali soltanto per campagne con obiettivo, pubblico e CTA definiti.
- Valutare l'eventuale offerta di supporto dopo la nascita prima di ripristinare una pagina pubblica dedicata; la bozza attuale resta fuori da route, navigazione e indicizzazione.

### P2 — estensioni del gestionale

- Multiaccount, ruoli e assegnazioni, esclusi dal lancio.

### P2 — pagamenti

- Prenotazione dedicata della consulenza dopo la call gratuita.
- Pagamento online per consulenze e corsi selezionati.
- Eventuale rateizzazione.

Non introdurre pagamenti prima che policy, stati, email, capienza e calendario siano stabili.

## Criteri di uscita per il lancio

Il sito può essere considerato pronto quando:

- non usa credenziali prevedibili in produzione;
- i flussi principali salvano sempre i dati e comunicano correttamente lo stato;
- testi, immagini e testimonianze sono autorizzati;
- homepage, corsi, sonno e prestazioni hanno gerarchie distinte;
- test automatici e controlli visuali sono superati;
- agenda admin, coda richieste e riconciliazione Calendar superano una giornata simulata completa anche su mobile;
- analytics e cookie rispettano il consenso;
- ciò che non è automatizzato rimanda in modo chiaro a telefono o WhatsApp;
- Selene è pronta a gestire manualmente il volume di richieste generato;
- la campagna iniziale dispone di creatività coordinate, percorso di conversione verificato e misurazione coerente con il consenso.

## Aggiornamento della roadmap

Quando un'attività viene conclusa, spostarla nella sezione completata indicando se è stata anche collaudata. Se una scelta cambia l'ambito o la priorità del progetto, registrarla prima in `DECISIONS.md`.
